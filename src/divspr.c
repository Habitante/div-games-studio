// Sprite generator -- removed (depended on visor/ 3D renderer, Phase 0 cleanup)
// Stubs kept for symbols still referenced by other modules

#include "global.h"

// Used by divbrush.c for scroll dragging
int last_x, last_y;

// Used by divpalet.c for 64-color thumbnail
byte *t64 = NULL;

void generador_sprites(void) {
  v_texto=(char *)texto[391]; dialogo(err0);
}
